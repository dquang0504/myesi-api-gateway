services:
  gateway:
    build: .
    environment:
      REDIS_URL: 
    container_name: myesi_gateway
    volumes:
      - .:/app
      - ./certs:/app/certs
      - ./logs:/app/logs
    ports:
      - "8000:8000"