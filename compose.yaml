services:
  gateway:
    build: .
    environment:
      REDIS_URL: localhost:6379
      JWT_SECRET: myesi_secret_key
      JWT_ALGORITHM: HS256
    container_name: myesi_gateway
    volumes:
      - .:/app
      - ./certs:/app/certs
      - ./logs:/app/logs
    ports:
      - "8000:8000"